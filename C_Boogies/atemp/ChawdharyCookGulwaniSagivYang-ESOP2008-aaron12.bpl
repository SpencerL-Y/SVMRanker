procedure main()
{
  var x: int;
  var y: int;
  var z: int;
  while ((x>=y))
  {
  if ((__VERIFIER_nondet_int()!=0))
  {
    x := (x+1);
    y := (y+x);  } else {
    x := (x-z);
    y := (y+(z*z));
    z := (z-1);  }
  }

}