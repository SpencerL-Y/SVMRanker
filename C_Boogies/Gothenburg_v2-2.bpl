procedure main()
{
  var x: int;
  var y: int;
  var a: int;
  var b: int;
  if (((a==(b+1))&&(x<0)))
  {
    while (((x>=0)||(y>=0)))
    {
    x := (((x+a)-b)-1);
    y := (((y+b)-a)-1);    }
  }

}